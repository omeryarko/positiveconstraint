# Claude Code personal settings — contains FTP credentials, never commit
.claude/settings.local.json

# Secrets — Buttondown API token, never commit
.claude/secrets/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor/OS junk
*.swp
*~
.publish-stage/
.publish-stage-redesign/

# Python virtualenv for GA4 / Search Console analytics tooling
.venv-analytics/

# Cloudflare Worker: local deps and wrangler state, never commit
cf-worker/node_modules/
cf-worker/.wrangler/
cf-worker/.dev.vars
cf-worker/worker-configuration.d.ts
cf-worker/scripts/__pycache__/
# Serve-signal data + logs: local-growing, back up separately (noisy in git)
cf-worker/serve-signal-history.jsonl
cf-worker/serve-signal-cron.log

# Claude Code worktree scratch
.claude/worktrees/
